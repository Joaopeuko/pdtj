[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pyyaml = "*"

[dev-packages]

[requires]
python_version = "3.8"


[scripts]
self_install = "pip install -e ."
update_requirements = "pipenv lock -r > requirements.txt"